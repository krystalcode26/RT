Homework 13

1. SQL vs NoSQL Database
SQL databases are relational databases that store data in tables (tabular) with predefined schemas. It use SQL and provide strong consistency through ACID transactions. Need to scan the whole table.
Hardware layer -> (hardisk->RAM-> application) Each of the row is written inside the hard disk. The, the hard disk is going to be read, Find that record loading into the memory, which is the RAM on your server and then execute it by whichever the TV program that's trying to access the data.
Ex: for banking and e-commerce systems,

NoSQL databases are non-relational and can store data as documents(mongoDB, DynamoDB), key-value pairs(Redis – cache layer, Riak), graphs(Neo4j, GraphDB), or wide-column(Cassandra, HBase) structures.
Redis -> in memory DB (RAM) -> persist RD in redis -> hardisk -> flush (1s) -> harddisk
Ex: big data and highly scalable applications.

2. What is Database Normalization?
Database normalization is the process of organizing data to reduce redundancy and improve data integrity. It divides large tables into smaller related tables and establishes relationships between them. 
1NF - sometimes when you are the normalized for the first normal form, you will introduce more rows. If the rows are significantly increase, then some of the read queries is going to take a longer time to execute.
2NF - second normalization form, you first have to be in first and normalization form which means that all of the columns are going to hold a single value for each of the racket. And then after that, you have to make sure only a single column is going to be primary key. 
3NF(in 2NF, has no transitive functional dependencies) Common normal forms include 1NF, 2NF, and 3NF, each removing different types of data duplication. Normalization makes databases easier to maintain and helps prevent update, insert, and delete anomalies.
 
3. Vertical Scaling vs Horizontal Scaling
Vertical scaling, also called scaling up, means increasing the resources of a single server, such as adding more CPU, memory, or storage. It is simple to implement but is limited by the hardware capacity of one machine. Horizontal scaling, also called scaling out, means adding more servers to distribute the workload. Horizontal scaling provides better fault tolerance and scalability but requires load balancing and distributed system management.
 
4. What is ACID?
ACID is a set of properties that ensure reliable database transactions. 
Atomicity guarantees that all operations in a transaction succeed or fail together. 
Consistency ensures that the database remains in a valid state before and after a transaction. Isolation prevents concurrent transactions from interfering with each other.
Durability ensures committed data remains stored even after a system failure.
 
5. What is CAP Theorem?
The CAP theorem states that a distributed system can only guarantee two of the following three properties at the same time: Consistency, Availability, and Partition Tolerance. 
Consistency means the synchronization of the data of the status in between different locations. Availability means the piece of data is always available or not. Partition tolerance. When a piece of data is lost missing. Since network partitions can always occur, most distributed systems choose either CP or AP. The CAP theorem helps architects make trade-offs when designing distributed databases.

-- create
CREATE TABLE EMPLOYEE (
  empid INTEGER PRIMARY KEY,
  empname TEXT NOT NULL,
  dept TEXT NOT NULL,
  age INT,
  salary DECIMAL(10,2),
  FOREIGN KEY (empid) REFERENCES DEPARTMENT(deptid)
);

CREATE TABLE DEPARTMENT(
  depid INTEGER PRIMARY KEY,
  deptname VARCHAR(50)
);
-- insert
INSERT INTO EMPLOYEE VALUES (1, 'Clark', 1, 25, 70000);
INSERT INTO EMPLOYEE VALUES (2, 'Dave', 2, 28, 80000);
INSERT INTO EMPLOYEE VALUES (3, 'Ava', 1, 34, 90000);

INSERT INTO DEPARTMENT VALUES (1, 'Sales');
INSERT INTO DEPARTMENT VALUES (2, 'Accounting');

-- Get only employee names and ages.
SELECT empName, age 
FROM EMPLOYEE; 

-- Find employees older than 30.
SELECT * 
FROM EMPLOYEE 
WHERE age > 30;

-- Find employees whose salary is greater than 80,000.
SELECT * 
FROM EMPLOYEE 
WHERE salary > 80000;

-- List employees ordered by age (ascending).
SELECT *
FROM EMPLOYEE 
--where must boolean
ORDER BY age ASC;

-- Get the top 3 highest-paid employees.
SELECT *
FROM EMPLOYEE
ORDER BY salary DESC
LIMIT 3;

-- Count total number of employees.
SELECT COUNT(empId) AS numEmployee
FROM EMPLOYEE;

-- Find the average salary of all employees.
SELECT AVG(salary) AS averageSalary
FROM EMPLOYEE;

-- List employee name with department name.
SELECT e.empName, d.deptName 
FROM EMPLOYEE e
JOIN DEPARTMENT d
ON e.deptId = d.deptId;

-- Find employees earning the highest salary.
SELECT * 
FROM EMPLOYEE 
WHERE salary = (
  SELECT MAX(salary)
  FROM EMPLOYEE
);

-- Find employees earning the second highest salary.
SELECT *
FROM EMPLOYEE
WHERE salary = (
  SELECT MAX(salary)
  FROM EMPLOYEE
  WHERE salary < (
    SELECT MAX( salary)
    FROM EMPLOYEE
  )
);

-- Find employees earning the third highest salary
SELECT *
FROM EMPLOYEE
WHERE salary = (
  SELECT MAX(salary)
  FROM EMPLOYEE
  WHERE salary < (
    SELECT MAX( salary)
    FROM EMPLOYEE
    WHERE salary < (
      SELECT MAX( salary)
      FROM EMPLOYEE
    )
  )
);

 



